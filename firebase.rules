rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to write to 'contacts', and anyone to read (optional)
    match /contacts/{docId} {
      allow create: if true; // During testing only — lock this down before production
      allow read: if false;
    }
  }
}
